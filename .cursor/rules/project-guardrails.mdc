---
description: 專案協作 guardrails：精簡變更、先確認現況再動手（中文敘述為主）
alwaysApply: true
---

# Project Guardrails（專案守則）

- 只改這次同意的範圍，不要順手「優化」無關程式。
- **現況優先（situation-first）**：動手前須核對目前實際行為（必要時讀檔／搜尋）；在對話中簡要說明現況、影響範圍與依賴（例如模組、API），再提出改法；不得只靠推測而無依據。除非使用者明文說「直接改」或屬零風險純文案，否則先取得同意再改碼；與下列條款並存時從嚴。
- **每次改碼前**：分析現況與影響 → 回報現況與預計方向 → 取得同意後再編輯。
- 能小改就不要大改；不順便加功能、不順便重構。
- 介面須符合既有設計語言（design language）：版型、配色、間距、互動；不要任意改視覺風格。
- 安全為最高優先：主動避免注入、XSS／CSRF／認證繞過、不安全的 DOM 渲染等攻擊面。
- 防 bug 即防安全：避免危險捷徑，以免惡意資料經資料流或 UI 被植入。

# Vibe Coding Flywheel（黏著迭代飛輪）

- 預設以小步、可上線（shippable）、可回滾（reversible）的迴圈作為 **MVP（最小可行產品）** 節奏；同一對話串依序切換 **functional hats（職能視角）**：**PM（產品經理）→ Engineer（工程師）→ UX（使用者體驗）→ QA（品質保證）**。可由同一代理人擔任，但產出需標註當下職能。
  1. **PM／Product**：目標與非目標、成功指標（success metrics）、優先級與取捨；開工前給建議。
  2. **Engineer**：實作計畫、觸及模組、資料流與 **API** 契約（API contract）；完成實際程式變更。
  3. **UX（desktop／行動）**：版面、可讀性、動效與狀態回饋、觸控／點擊區（tap targets）；遵守既有 design language（見上方 UI 守則）。
  4. **QA**：逐步檢查與邊界情境；缺陷以重現步驟（repro steps）、預期／實際（expected／actual）回報；修復後依驗收標準（acceptance）核對。
- 每一輪 flywheel 結束須至少一項可驗證（verifiable）成果；避免一次承諾跨層大改而無中途檢查點（checkpoint）。

# Product Context（產品脈絡）

- 本專案為 **SaaS** 多租戶內容平台。
- 已支援多平台發布，部分能力仍在迭代。
- 任何改動不得破壞租戶隔離（tenant isolation）。

# Frontend Surfaces（前端介面概覽）

- 租戶主應用由 **`index.html`** 載入，使用 **`js/`** 下 **ES modules**（例如 `agent_v9_core.js`、`v9_state.js`、`v9_funnel*.js`、`api.js`、`config.js`）；後端基底網址在 `js/config.js` 的 `CONFIG.CLOUD_RUN_URL`。
- 環境或網域變更時優先核對 `config.js`。
- `js/main.js` 可能為舊版啟動路徑；除非任務明指 `main.js`，否則以 **`index.html`** 及其 inline **module** 匯入鏈為準；改動前先確認是否仍被引用。
- `s_admin/` 為獨立超管（super-admin）介面（自有 HTML／JS）；管理流程與憑證須與租戶應用隔離。

# Dual-Product Architecture Rules（雙產品線架構）

- 清楚區隔產品邊界：**Meta** 平台流程與通用內容生成為不同產品線，勿混用。
- 可重用能力（腳本、分鏡、生成編排、素材生命週期）應模組化為服務，供內部 **API** 或 **Agent** 呼叫。
- 凡涉及任務、素材、憑證、發布紀錄，在資料／模型／查詢邊界落實租戶隔離。
- 平台專屬邏輯（**Meta** token、發布政策、endpoint 契約）收在 **adapter** 式模組後方，勿洩漏進核心生成邏輯。
- 任務關鍵流程須可追溯、可回滾：以 **taskId** 為基礎的狀態轉移、稽核紀錄、對發布／計費敏感動作採 **idempotent（冪等）** 操作；重試不得造成重複扣費或重複發布。

# Point Deduction Rule（扣點規則）

- 扣點與定價邏輯以後端為準。
- 涉及扣點行為時，先取得後端程式／脈絡再動手。
- 勿只憑前端臆測規則，或單方面改點數規則。

# Anti-Drift Execution Rules（防漂移執行）

- 實作前鎖定成果：每次任務須有一個可衡量成果與明確「非目標」。
- 跨流程／多模組改動須先過迷你規格（mini spec）：目標、邊界、驗收（acceptance）、風險。
- 計畫即執行契約：實作前列出觸及模組、資料流、失敗處理與回滾路徑。
- 驗收優先：寫碼前先定測試／驗證步驟與通過標準；實作須對應驗收項。
- 歧義早升級：需求衝突、限制不清或風險不明時，先釐清再編輯，勿硬做。

# Glossary（新手向名詞）

- **BMad**（Breakthrough Method for Agile AI-Driven Development）：把 **AI** 當成小團隊協作（**PM**、工程師、**QA** 等分工），逐步完成需求。
- **SpecKit**：需求寫清楚再開工的流程（Rule → Spec → Plan → Tasks），避免 **AI** 做偏。
- **Rule**：先訂「不能踩線」與「必須做到」的原則。
- **Spec**：本功能做什麼、不做什麼、怎樣算完成。
- **Plan**：技術上改哪裡、資料怎麼走、失敗如何補救。
- **Tasks**：拆成可逐項完成的小步驟。

# Onion Theory（洋蔥理論執行）

- 由核心目標向外逐層完成，不要一次全切。
- 每一層要小、可測、可逆；穩定後再進下一層。
- 若改動可能動搖架構穩定，先拆更小迭代。
- 寧可先確保方向正確，再追速度；避免「快但脆弱（fast but fragile）」的交付。
